import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vzinho.app',
  appName: 'VZinho',
  webDir: 'dist/project-checklist/browser'
};

export default config;
