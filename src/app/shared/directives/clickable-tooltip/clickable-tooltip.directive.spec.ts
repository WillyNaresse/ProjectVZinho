import { ClickableTooltipDirective } from './clickable-tooltip.directive';

describe('ClickableTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
